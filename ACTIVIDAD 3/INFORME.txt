se completaron las pruebas automatizadas para el método calculate() de la clase ChocolateBags, utilizando pruebas parametrizadas con JUnit 5. Se cubrieron las cuatro particiones identificadas: cuando el total es mayor que la cantidad disponible de barras (totalIsTooBig), cuando se utilizan solo barras grandes (onlyBigBars), cuando se requieren tanto barras grandes como pequeñas (bigAndSmallBars), y cuando se utilizan únicamente barras pequeñas (onlySmallBars). Además, se incluyeron casos en los límites de cada partición para asegurar una mayor cobertura. La ejecución de todas las pruebas validó que el sistema se comporta correctamente en cada escenario considerado.

